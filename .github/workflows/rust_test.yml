# .github/workflows/rust-test.yml
name: Build Rust app

on:
  workflow_call:
    inputs: 
      runner:
        required: true
        type: string

jobs:
  test-app:
    name: Testing
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2
        
      - name: Check formatting
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo fmt --all -- --check

      - name: Run Clippy linter
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo clippy --all-targets --all-features -- -D warnings

      - name: Run unit, integration, and doc tests
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo test --all --verbose

      # Inline code‑coverage without an extra action
      - name: Install coverage tool
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo install cargo-tarpaulin

      - name: Generate coverage report
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo tarpaulin --ignore-tests --out Html --out Xml

      - name: Upload coverage artifacts
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report
          path: |
            kataglyphis-rustprojecttemplate/tarpaulin-report.html
            kataglyphis-rustprojecttemplate/tarpaulin-report.xml

      - name: Fuzz tests
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo install cargo-fuzz
          cargo fuzz run fuzz_target

      - name: Run benchmarks
        run: |
          cd kataglyphis-rustprojecttemplate
          cargo bench
